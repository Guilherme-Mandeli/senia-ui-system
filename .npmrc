# Bypasses Windows network share permission errors by hoisting packages
node-linker=hoisted
symlink=false

# Authorizes esbuild compilation scripts under pnpm secure sandbox
only-built-dependencies[]=esbuild
